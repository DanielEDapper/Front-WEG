window.addEventListener("scroll", () => {
    console.log(window.scrollY);
})

const process = document.querySelector(".process");
console.log(process.offSetTop
    const distance =
window.scrollY - process.offsetTop;
);