import "./Resumo.css";

interface ResumoProps{
    total : number
    operando : number
    atencao : number
    parados : number
};

function Resumo({total, operando, atencao, parados} : ResumoProps)
{

}